{# if reservaciones | length > 0 #}
<div class="col-md-6 portlets ui-sortable">
    <div class="widget">    
        <div style="background-color: #5cb85c; color: white; text-align: center;font-size: 20px; padding: 10px;">    
            Mis reservaciones    
        </div>
        <div class="widget-content">
            <div class="padd">
                {% include 'FrontendBundle:Reservacion:lista.html.twig' with {'reservaciones': reservaciones } %}
                <br/>
                <button class="btn btn-success pull-left" id="botonPagoReservacion">Subir comprobante</button>
                <a href="{{path('reservaciones_calendario_select_recurso')}}" class="btn btn-success pull-right" id="crearReservacion">Crear reservación</a>
                <div class="clearfix"></div>
            </div>
        </div>
    </div>
</div>
{# endif #}