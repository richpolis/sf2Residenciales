{% extends '::base.html.twig' %}

{% block body -%}
    <h1>EstadoCuenta list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cargo</th>
                <th>Tipo</th>
                <th>Createdat</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('estadodecuentas_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.cargo }}</td>
                <td>{{ entity.tipo }}</td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('estadodecuentas_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('estadodecuentas_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('estadodecuentas_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
